
name: Action

on:
  watch:
    types: [started]
#  push:
#    branches: [ master ]
#  pull_request:
#    branches: [ master ]
   
env:
  WeTransfer: true

jobs:
    build:
      runs-on: ubuntu-20.04

      steps:
        - name: 获取本仓库源码
          uses: actions/checkout@main
  
        - name: 清理环境
          run: |
             git clone https://github.com/zhlhlf/jczhl-package
             mkdir 666
             mv jczhl-package/HC5861 666
             cp 666/HC5861/board.h board.h
             ls   
             ls 666     
             
